# Example-of-Macro-based-automation-python
what it does is on the tin, simply uses PYautogui Write tool and PYgetwindow to grab and then type into a window (look at source code for details) does NOT work as a compiled EXE AND may not compatible with LINUX due to the use of PYgetwindow which is not supported, this is SOMEWHAT cases sensitive as it relies on the USERs PC to be able to boot COMMAND prompt from the SYS32 directory within  700MS and  NOTEPAD within 200ms

